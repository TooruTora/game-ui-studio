---
description: 매니페스트 JSON을 HTML 프리뷰로 렌더하고 깨진 레이아웃 경고를 수집합니다.
argument-hint: "<manifest-path> [catalog-path]"
---

매니페스트 경로(필수)와 카탈로그 경로(선택)를 web-preview 스킬로 위임합니다.

`/quartermaster:web-preview $ARGUMENTS`
